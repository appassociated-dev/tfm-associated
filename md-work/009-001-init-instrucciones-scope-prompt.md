Quiero inicializar los *scopes* del proyecto con sus instrucciones correspondientes.

1. Analiza el reporte `md-work/004-002-reporte-scopes-instrucciones-skills.md`.

2. Revisa el archivo `AGENTS.md` del *scope* root y complétalo con la información que falte, **omitiendo todo lo relacionado con los skills**.

3. Genera una primera versión de las instrucciones (sin tener en cuenta los *skills*, únicamente la cabecera y estructura base) para los siguientes *scopes*:

   * `api/AGENTS.md`
   * `web/AGENTS.md`
   * `e2e/AGENTS.md`

Del mismo modo que el archivo `AGENTS.md` del *scope* root, las instrucciones deben generarse en **inglés**.

Además, incluye los bloques correspondientes a *skills*, pero dejando las tablas **vacías** (sin contenido). Posteriormente se completarán.

### Ejemplo de bloques de skills vacíos

```markdown
---

## Available Skills

Use these skills for detailed patterns on-demand:

### Generic Skills (Any Project)
| Skill | Description | URL |
|-------|-------------|-----|

### Auto-invoke Skills

When performing these actions, ALWAYS invoke the corresponding skill FIRST:

| Action | Skill | Examples |
|--------|-------|----------|

---

```
