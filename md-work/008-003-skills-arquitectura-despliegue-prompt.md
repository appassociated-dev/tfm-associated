Entiendo, por tanto, que:

1. Los *skills* que yo desarrolle deberían estar ubicados en la carpeta `skills/` de mi repositorio y contar con algún script que los copie a las carpetas de los agentes que quiera soportar (`.agents/`, `.claude/`, `.gemini/`, etc.).

2. Los *skills* que utilice desde la comunidad residirían únicamente en las carpetas de los agentes que desee soportar (`.agents/`, `.claude/`, `.gemini/`, etc.).

3. Las carpetas de *skills* de los agentes que quiera soportar deberían estar excluidas en el `.gitignore`, documentando además el proceso de despliegue en local para que un desarrollador externo pueda trabajar en el repositorio.

¿Es correcto este planteamiento?
