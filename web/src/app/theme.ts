import { createTheme } from '@mantine/core';

/**
 * Tema personalizado de Mantine para Associated.
 * Se puede extender con colores, tipografía y componentes específicos.
 */
export const theme = createTheme({
  primaryColor: 'blue',
  fontFamily:
    '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif',
  defaultRadius: 'md',
});
